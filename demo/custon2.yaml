_BASE_: "../configs/Base-RCNN-FPN.yaml"
INPUT:
  FORMAT: BGR
MODEL:
  WEIGHTS: "50k_iter.pth"
  MASK_ON: True
  RESNETS:
    DEPTH: 50
  PIXEL_MEAN: [30.0, 30.0, 30.0]
DATASETS:
  TEST: [for_detectron_val,]

